{
	"name": "@wpackages/dev-kit",
	"version": "0.0.1",
	"type": "module",
	"private": true,
	"engines": {
		"bun": ">=1.0.0"
	},
	"description": "Dev Kit - Unified interface for testing, TypeScript build, formatting, and reporting",
	"main": "./dist/index.mjs",
	"module": "./dist/index.mjs",
	"types": "./dist/index.d.mts",
	"packageManager": "bun@1.3.7",
	"scripts": {
		"dev": "bun --watch src/index.ts",
		"format": "dprint fmt",
		"lint": "oxlint --fix --type-aware",
		"build": "bun build src/index.ts --outdir dist --target bun --format esm && bun build src/index.ts --outdir dist --target bun --format esm --sourcemap",
		"test": "bun test",
		"verify": "bun run format && bun run lint && bun run test && bun run build"
	},
	"keywords": [
		"dev",
		"kit",
		"test",
		"typescript",
		"build",
		"formatter",
		"reporter",
		"development"
	],
	"author": "wrikka",
	"license": "MIT",
	"dependencies": {
		"@wpackages/formatter": "workspace:*",
		"@wpackages/reporter": "workspace:*",
		"@wpackages/test": "workspace:*",
		"@wpackages/ts-build": "workspace:*"
	},
	"devDependencies": {
		"@types/bun": "^1.3.6",
		"oxlint": "^1.42.0",
		"typescript": "^5.9.3"
	},
	"exports": {
		".": "./dist/index.mjs",
		"./package.json": "./package.json"
	}
}
