//! dsl/grammar.rs

// Placeholder for the DSL's grammar and Abstract Syntax Tree (AST) nodes.

/// Represents the root of an agent definition in the DSL.
pub struct AgentDefinition {
    pub name: String,
    // Other fields representing the agent's structure will be added here.
}
