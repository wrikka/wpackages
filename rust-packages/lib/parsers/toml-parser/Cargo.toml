[package]
name = "toml-parser"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }
toml = "0.9.8"
napi = { version = "2", default-features = false }
napi-derive = "2"

[build-dependencies]
napi-build = "2"
