# Tracing Configuration for Cache Library

# Set default log level
# Options: trace, debug, info, warn, error
default = "info"

# Cache-specific log levels
cache = "info"
cache::in_memory = "debug"
cache::disk = "debug"
cache::traits = "info"
cache::metrics = "info"

# External dependencies
tokio = "info"
moka = "warn"
sled = "warn"
