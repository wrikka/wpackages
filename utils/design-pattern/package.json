{
	"name": "@wpackages/design-pattern",
	"version": "0.1.0",
	"private": true,
	"description": "Collection of design patterns implemented in TypeScript",
	"type": "module",
	"main": "./dist/index.mjs",
	"module": "./dist/index.mjs",
	"types": "./dist/index.d.mts",
	"exports": {
		".": "./dist/index.mjs",
		"./package.json": "./package.json"
	},
	"files": ["dist", "README.md"],
	"scripts": {
		"build": "tsdown --exports --dts --minify",
		"dev": "tsdown --watch",
		"test": "vitest run",
		"test:watch": "vitest",
		"format": "dprint fmt",
		"lint": "tsc --noEmit && oxlint --fix",
		"verify": "bun run format && bun run lint && bun run build && bun run test"
	},
	"dependencies": {},
	"devDependencies": {
		"@types/bun": "^1.3.5",
		"@types/node": "^25.0.5",
		"oxlint": "^1.38.0",
		"tsdown": "^0.18.4",
		"typescript": "^5.9.3",
		"vitest": "^4.0.16"
	}
}
