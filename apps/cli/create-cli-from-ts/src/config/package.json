{
  "name": "@wpackages/config",
  "version": "0.0.1",
  "type": "module",
  "description": "__DESCRIPTION__",
  "keywords": [
    "cli",
    "typescript",
    "bundler"
  ],
  "bin": {
    "__BIN_NAME__": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js",
    "./package.json": "./package.json"
  },
  "scripts": {
    "dev": "bun run src/index.ts",
    "lint": "tsc --noEmit && oxlint .",
    "build": "tsdown",
    "test": "vitest --run --passWithNoTests",
    "format": "dprint fmt",
    "verify": "bun format && bun lint && bun run test && bun audit && bun run build",
    "binary:prepare": "node --experimental-sea-config sea-config.json",
    "binary:package": "node -e \"const fs = require('fs'); const path = require('path'); const binDir = path.join('dist-binary'); if (!fs.existsSync(binDir)) fs.mkdirSync(binDir); fs.copyFileSync(process.execPath, path.join(binDir, '__BIN_NAME__'));\" && npx postject ./dist-binary/__BIN_NAME__ NODE_SEA_BLOB ./dist/sea-prep.blob --sentinel-fuse NODE_SEA_FUSE_fce680ab2cc467b6e072b8b5df1996b2",
    "binary": "bun run build && bun run binary:prepare && bun run binary:package",
    "sea:prepare": "node --experimental-sea-config sea-config.json",
    "sea:package": "npx postject ./dist/index.js NODE_SEA_BLOB ./dist/sea-prep.blob --sentinel-fuse NODE_SEA_FUSE_fce680ab2cc467b6e072b8b5df1996b2",
    "sea": "bun run build && bun run sea:prepare && bun run sea:package",
    "watch": "bun --watch src/index.ts"
  },
  "dependencies": {
    "@wpackages/cli-builder": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "^1.3.8",
    "dprint": "^0.50.2",
    "oxlint": "^1.42.0",
    "oxlint-tsgolint": "^0.10.1",
    "postject": "^1.0.0-alpha.6",
    "tsdown": "^0.18.4",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  },
  "packageManager": "bun@1.3.8",
  "author": "Unknown",
  "license": "MIT"
}
