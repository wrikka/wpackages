{
	"name": "my-cli",
	"version": "1.0.0",
	"description": "A CLI tool for development tasks",
	"type": "module",
	"bin": "./dist/index.js",
	"scripts": {
		"dev": "bun --watch src/index.ts",
		"build": "tsdown --exports --dts",
		"lint": "tsc --noEmit && oxlint --fix",
		"test": "echo \"Error: no test specified\" && exit 1",
		"check": "bun run lint"
	},
	"dependencies": {
		"commander": "14.0.2",
		"picocolors": "^1.1.1"
	},
	"devDependencies": {
		"@types/bun": "latest",
		"oxlint": "^1.35.0",
		"tsdown": "^0.18.3",
		"typescript": "^5.9.3"
	},
	"author": "",
	"license": "ISC",
	"repository": {
		"type": "git",
		"url": ""
	}
}
