/**
 * Queue size and capacity constants
 */

/** Maximum queue size for bounded queues */
export const MAX_QUEUE_SIZE = 1000;

/** Default queue size when not specified */
export const DEFAULT_QUEUE_SIZE = 100;
