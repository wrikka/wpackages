id: typescript-patterns-rule
language: typescript
rule:
  pattern: import type { $$$$IMPORTS } from "$MODULE"

  message: |
    Use 'import type' for type-only imports to improve tree-shaking and reduce bundle size.
    This is especially important for plugin development.

severity: suggestion
