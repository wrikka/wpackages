import { longestPalindromicSubsequence } from "./longest-palindromic-subsequence";

const str = "bbbab";
const length = longestPalindromicSubsequence(str);

console.log("String:", str);
console.log("Length of longest palindromic subsequence:", length);
