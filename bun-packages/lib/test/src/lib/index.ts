/**
 * Third-party library wrappers
 */

// Vitest utilities are optional and only exported if vitest is installed
// Users can import directly from vitest if needed
// Example: import { describe, it, test } from 'vitest'

// Placeholder export to avoid empty file
export const VITEST_INTEGRATION = true;
