{
	"name": "reactivity",
	"version": "0.1.0",
	"description": "Simple reactive primitives for functional programming",
	"keywords": [
		"reactive",
		"functional",
		"signals",
		"state",
		"reactivity"
	],
	"type": "module",
	"main": "./dist/index.js",
	"types": "./dist/index.d.ts",
	"exports": {
		".": "./dist/index.js",
		"./package.json": "./package.json"
	},
	"files": [
		"dist",
		"README.md"
	],
	"scripts": {
		"build": "tsdown --exports --dts --minify",
		"dev": "tsdown --watch",
		"test": "vitest run",
		"lint": "tsc --noEmit && oxlint --fix",
		"review": "bun run format && bun run lint && bun run test && bun run build"
	},
	"devDependencies": {
		"@types/bun": "^1.3.5",
		"tsdown": "^0.15.12",
		"typescript": "^5.9.3",
		"vitest": "^4.0.16"
	},
	"module": "./dist/index.js"
}
