import { countingSort } from "./counting-sort";

// Usage example for countingSort
const unsortedArray = [4, 2, 2, 8, 3, 3, 1];
const sortedArray = countingSort(unsortedArray);

console.log("Sorted array:", sortedArray);
