{
	"name": "git",
	"version": "0.1.0",
	"description": "A TypeScript library for interacting with Git repositories.",
	"type": "module",
	"main": "./dist/index.js",
	"module": "./dist/index.js",
	"types": "./dist/index.d.ts",
	"exports": {
		".": "./dist/index.js",
		"./package.json": "./package.json"
	},
	"scripts": {
		"build": "tsdown --exports --dts --minify",
		"dev": "bun --watch src/index.ts",
		"lint": "tsc --noEmit && oxlint --fix",
		"review": "bun format && bun lint && bun test",
		"test": "vitest"
	},
	"dependencies": {
		"functional": "workspace:*"
	},
	"devDependencies": {
		"@types/bun": "latest",
		"@types/node": "^24.10.4",
		"knip": "^5.77.4",
		"oxlint": "1.28.0",
		"tsdown": "^0.15.12",
		"typescript": "^5.9.3",
		"vitest": "^4.0.16"
	}
}
