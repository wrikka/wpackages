// TODO: effect not available - Layer.succeed not available
// import { makeHistoryService } from "./factory";
// import { HistoryService } from "./service";

// export const HistoryServiceLive = (maxSize = 100) => Layer.succeed(HistoryService, makeHistoryService(maxSize));

export {};
