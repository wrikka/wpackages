// TODO: effect not available - Context.Tag not available
// import type { CanvasState } from "../state";

// export class ExportService extends Context.Tag("ExportService")<
// 	ExportService,
// 	{
// 		toJSON: (state: CanvasState) => Effect.Effect<string>;
// 		fromJSON: (json: string) => Effect.Effect<CanvasState>;
// 		toSVG: (state: CanvasState) => Effect.Effect<string>;
// 		toPNG: (
// 			state: CanvasState,
// 			canvas: HTMLCanvasElement,
// 		) => Effect.Effect<Blob>;
// 	}
// >() {}

export type ExportService = any;
